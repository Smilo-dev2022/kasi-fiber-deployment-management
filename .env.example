# Example env for local dev (copy to .env)
# Never commit real secrets.

# Supabase project
SUPABASE_URL=https://bigbujrinohnmoxuidbx.supabase.co
SUPABASE_PROJECT_REF=bigbujrinohnmoxuidbx
SUPABASE_ANON_KEY=replace
# Note: SUPABASE_ACCESS_TOKEN is set as a GitHub Secret for CI

# Database (FastAPI)
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql+psycopg2://app:${POSTGRES_PASSWORD}@db:5432/app
CORS_ALLOW_ORIGINS=http://localhost:3000
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=replace_with_strong_secret

# Object storage (MinIO/S3)
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=fiber-photos
S3_ACCESS_KEY=minio
S3_SECRET_KEY=change-me

# Legacy Node API
MONGODB_URI=mongodb://localhost:27017/kasi_fiber_db
MAIL_FROM=no-reply@kasi-fiber.local
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
DISABLE_SLA_MONITOR=true
SLA_MONITOR_INTERVAL_MS=60000
NMS_WEBHOOK_SECRET=change-me
