# Copy this file to `.env` and fill in local values.
# Never commit real secrets.

# Supabase project configuration
SUPABASE_URL=https://bigbujrinohnmoxuidbx.supabase.co
SUPABASE_PROJECT_REF=bigbujrinohnmoxuidbx
SUPABASE_ANON_KEY=replace_me
SUPABASE_SERVICE_ROLE_KEY=replace_me

# Note: SUPABASE_ACCESS_TOKEN is managed in GitHub Secrets for CI.
# Database
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql+psycopg2://app:${POSTGRES_PASSWORD}@db:5432/app

# Object storage (Minio/S3)
S3_ENDPOINT=http://minio:9000
S3_REGION=us-east-1
S3_BUCKET=fiber-photos
S3_ACCESS_KEY=minio
S3_SECRET_KEY=change-me
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=change-me

# Supabase (client)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=

# Webhook / HMAC secrets
NMS_HMAC_SECRET=change-me
OXIDIZED_HMAC_SECRET=change-me

# CORS
CORS_ALLOW_ORIGINS=http://localhost:3000
