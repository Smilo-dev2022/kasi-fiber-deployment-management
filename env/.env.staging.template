APP_ENV=staging
PORT=8000
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/fiber_staging
JWT_SECRET=${SECRET_STAGING_JWT}
JWT_ALG=HS256
WEBHOOK_HMAC_LIBRENMS=${SECRET_STAGING_HMAC_LIBRENMS}
WEBHOOK_HMAC_ZABBIX=${SECRET_STAGING_HMAC_ZABBIX}
S3_ENDPOINT=http://localhost:9000

S3_REGION=us-east-1
S3_BUCKET=fiber-staging
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

TIMEZONE=Africa/Johannesburg
SENTRY_DSN=${SECRET_STAGING_SENTRY_DSN}
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=${SECRET_STAGING_SMTP_USER}
SMTP_PASS=${SECRET_STAGING_SMTP_PASS}
EMAIL_FROM=noreply-staging@yourdomain.co.za

OIDC_ISSUER_URL=${SECRET_STAGING_OIDC_ISSUER}
OIDC_CLIENT_ID=${SECRET_STAGING_OIDC_CLIENT_ID}
OIDC_CLIENT_SECRET=${SECRET_STAGING_OIDC_CLIENT_SECRET}

